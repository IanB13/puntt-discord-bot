{
  "name": "puntt-discord-bot",
  "version": "1.0.0",
  "description": "Discord bot for esports betting info",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "watch": "nodemon index.js",
    "build:image": "docker build -t bet-bot .",
    "run:docker": "docker run -it --env-file .env  -d bet-bot",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ian Blair",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^12.4.1",
    "dotenv": "^8.2.0",
    "mongodb": "^3.6.3",
    "mongoose": "^5.10.15",
    "puppeteer": "^5.5.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
